services:
  db:
    image: mariadb:11
    environment:
      MARIADB_DATABASE: trackfinder
      MARIADB_USER: trackuser
      MARIADB_PASSWORD: trackpass
      MARIADB_ROOT_PASSWORD: rootpass
    tmpfs:
      - /var/lib/mysql
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s

  backend:
    image: ghcr.io/juanje-urban/trackfinder-garage-backend:pec3
    environment:
      SPRING_PROFILES_ACTIVE: demo
      SPRING_DATASOURCE_URL: jdbc:mariadb://db:3306/trackfinder
      SPRING_DATASOURCE_USERNAME: trackuser
      SPRING_DATASOURCE_PASSWORD: trackpass
    ports:
      - "8080:8080"
    depends_on:
      db:
        condition: service_healthy

  frontend:
    image: ghcr.io/juanje-urban/trackfinder-garage-frontend:pec3
    ports:
      - "5173:80"
    depends_on:
      - backend
